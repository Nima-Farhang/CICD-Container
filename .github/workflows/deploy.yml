name: Push to prod branch on master changes

on:
  push:
    branches: [ master ]

jobs:
  push-to-prod:
    runs-on: ubuntu-latest
    steps:
      - name: test source
        run: |
          ls -la
          pwd
          hostname

